<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>11/05/25</title>
  <style>
    @keyframes blinkColor {
      0% { color: red; }
      50% { color: pink; }
      100% { color: orange; }
    }

    @keyframes slideInLeft {
      0% { transform: translateX(-100%); opacity: 0; }
      100% { transform: translateX(0); opacity: 1; }
    }

    @keyframes slideInRight {
      0% { transform: translateX(100%); opacity: 0; }
      100% { transform: translateX(0); opacity: 1; }
    }

    .blink {
      animation: blinkColor 2s infinite;
    }

    .slide-left {
      animation: slideInLeft 2s forwards;
    }

    .slide-right {
      animation: slideInRight 2s forwards;
    }
  </style>
</head>
<body style="display: flex; flex-direction: column; justify-content: center; align-items: center; height: 100vh; margin: 0; font-family: monospace; background-color: white;">

  <!-- Text for each line -->
  <div class="slide-left" style="font-size: 60px;">Minakshi (Since 1975)</div>
  <div class="blink" style="font-size: 100px; font-weight: bold;">Happy Mother's Day</div>
  <div class="slide-right" style="font-size: 60px;">Mausumi (Since 1976)</div>

</body>
</html>
